
nl.dekkr.pagefetcher {
  api {
    host = "localhost"
    port = 8080
    timeout = 5 #seconds
  }
  persistence {
    flavor = "memory"  # postgres / mongo / prevayler / memory
  }
}

akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}
